package com.glivt.user;

/**
 * Visible roles preserved from the existing product. Distributor / Manager /
 * Customer behaviours are represented through the granular permission object,
 * not through additional visible roles.
 */
public enum Role {
    SUPER_ADMIN,
    ADMIN,
    DRIVER
}
